/**
 * Kích thước icon chuẩn (Lucide React) — dùng thống nhất trên toàn web
 * sm: 16 — inline nhỏ (filter, dropdown, breadcrumb)
 * md: 18 — menu danh mục, thanh nav phụ
 * lg: 20 — nút nav chính, đóng modal
 * xl: 24 — mobile menu, CTA lớn
 */
export const ICON_SIZE = {
  sm: 16,
  md: 18,
  lg: 20,
  xl: 24,
} as const;
